import { AsyncHostInterface, WorkerInterface, WorkerRPC } from '../chrome-cxx/mnt/extension/WorkerRPC';
export type * from '../chrome-cxx/mnt/extension/WorkerRPC';
/** Utility type to get the worker's return value from a method name. */
export type MethodReturn<T extends keyof WorkerInterface> = ReturnType<WorkerInterface[T]> extends Promise<infer R> ? R : ReturnType<WorkerInterface[T]>;
export interface IWasmWorker {
    rpc: WorkerRPC<AsyncHostInterface, WorkerInterface>;
    dispose(): Promise<void>;
}
export declare function spawn(hostInterface: AsyncHostInterface): IWasmWorker;
